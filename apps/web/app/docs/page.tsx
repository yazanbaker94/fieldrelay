import { PrototypeDisclaimer, PublicHeader } from '@/components/fieldrelay/public-header';

const endpoints = ['POST /api/v1/demo/runs','POST /api/v1/sync/operations','GET /api/v1/sync/operations/:id','GET /api/v1/shipments','GET /api/v1/shipments/:id','POST /api/v1/handoffs/:token/receive','POST /api/v1/exceptions/:id/resolve','POST /api/v1/integrations/deliveries/:id/replay','GET /api/v1/audit','GET /api/v1/events'];

export default function DocsPage() {
  return <main className="public-inner"><PublicHeader /><section className="docs-layout"><aside><p>FR / DOCS / 01</p><nav><a href="#run">Run locally</a><a href="#model">Domain model</a><a href="#sync">Offline sync</a><a href="#api">API surface</a><a href="#delivery">Delivery recovery</a><a href="#limits">Prototype limits</a></nav></aside><article className="docs-article"><header><p>Implementation notes / reviewer edition</p><h1>Technical decisions,<br />without the hand-waving.</h1><p>This compact guide explains how the demo preserves evidence across device, database, queue, and destination boundaries.</p></header><section id="run"><span>01</span><h2>Run locally</h2><pre>npm install{`\n`}npm run dev{`\n\n`}# Web     http://localhost:3000{`\n`}# API     http://localhost:4100{`\n`}# OpenAPI http://localhost:4100/docs</pre></section><section id="model"><span>02</span><h2>Four independent state dimensions</h2><div className="state-code"><code>lifecycle  DRAFT → OFFERED → ACCEPTED → PICKED_UP → IN_TRANSIT → RECEIVED → COMPLETED</code><code>sync       SAVED_ON_DEVICE → WAITING → SYNCING → SYNCED | NEEDS_REVIEW</code><code>exception  NONE → DISCREPANCY_OPEN → RESOLVED</code><code>delivery   NOT_STARTED → PENDING → RETRYING → FAILED → DLQ → DELIVERED</code></div></section><section id="sync"><span>03</span><h2>Unknown outcome ≠ failed save</h2><p>If the server applies an offline operation but its response is lost, the client moves to <strong>Checking sync result</strong>. It queries or retries with the same idempotency key. The API returns the original result, so the mutation is not repeated.</p><blockquote>Conflict handling never overwrites the server silently: send the local entry for review, keep it as a separate draft, or use the server version.</blockquote></section><section id="api"><span>04</span><h2>Representative API</h2><ul className="endpoint-list">{endpoints.map((endpoint) => <li key={endpoint}><code>{endpoint}</code></li>)}</ul></section><section id="delivery"><span>05</span><h2>Transactional completion and recovery</h2><p>Resolving EX-0037 appends the resolution, advances FR-2026-0842 to Completed, and inserts the outbox record in one transaction. The worker uses a stable destination key through retries, DLQ movement, and manual replay.</p><a className="docs-link" href="/app/integrations/DL-019">Inspect delivery DL / 019 →</a></section><section id="limits"><span>06</span><h2>Honest prototype limits</h2><p>The demo uses synthetic data and illustrative validation rules. Local queue adapters stand in for cloud resources when AWS credentials are absent. OData is an example enterprise adapter only. No claim is made about WiQ’s private architecture, integrations, or regulatory implementation.</p></section></article></section><PrototypeDisclaimer /></main>;
}
